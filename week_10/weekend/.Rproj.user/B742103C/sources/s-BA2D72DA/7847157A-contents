---
title: "R Notebook"
output: html_notebook
---

```{r}
library(tidyverse)
library(janitor)
library(lubridate)
```

```{r}
avocados <- read_csv(here::here("data/avocado.csv")) %>% 
  clean_names()
```

```{r}
# extract month from date column, then remove date column
avocados %>% 
  mutate(month = month(date, label = TRUE, abbr = TRUE), .after = 1) %>% 
  select(-date)
```

